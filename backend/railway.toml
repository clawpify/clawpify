# Railway picks up Dockerfile when present. Service **Root Directory** should be `backend/`
# (see https://docs.railway.com/guides/monorepo). Config-as-code overrides dashboard commands.
[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/usr/local/bin/backend"
